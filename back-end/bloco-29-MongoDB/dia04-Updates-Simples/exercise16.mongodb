use('class');
db.xmen.updateMany(
    {  $or: [

        { class: 'omega' },
        { class: 'gama' }
    ]
    },
    { 
        $currentDate: {
            lastUpdate: { $type: 'timestamp' },
        },
        $max: {
            power: 500
        }
    }
);

use('class');
db.xmen.find(
    {  $or: [

        { class: 'omega' },
        { class: 'gama' }
    ]
    }
);